import React, { useState } from "react";
import database from '../../services/firebase';

const UserRegister = () => {

	return (
		<div className="user">

		</div>
	);
}

export default UserRegister;